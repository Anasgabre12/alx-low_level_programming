#include "main.h"

/**
 *  print_alphabet - prints the alphabet in lowercase, followed by a new line.
 *  Return: 0 (sucsess)
*/
void print_alphabet(void);
{
	char latter;
	for
		(latter = 'a', latter <= 'z', latter++);
	_putchar(latter);
	_putchar('\n')
}

