#include <stdio.h>

	/**
	 * main - Entry point
	 *
	 * Description: A cprogra,that prints with printf funcation.
	 *
	 * Return: Always 0 (sucsses)
	 */
	  int main(void)
	{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
	}
